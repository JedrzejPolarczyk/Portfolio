<Window x:Class="YourNamespace.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <telerik:RadGridView x:Name="BookshelfGrid" AutoGenerateColumns="True">
            <!--<telerik:RadGridView.Columns>
                <telerik:GridViewDataColumn Header="Stand" DataMemberBinding="{Binding Stand}" />
                <telerik:GridViewDataColumn Header="Shelf" DataMemberBinding="{Binding Shelf}" />
                <telerik:GridViewDataColumn Header="Main Item Name" DataMemberBinding="{Binding MainItemName}" />
            </telerik:RadGridView.Columns>-->
        </telerik:RadGridView>
    </Grid>
</Window>
